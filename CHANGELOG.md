# 更新日志

本扩展的版本更新记录（新版在上）。

## 1.6.2 — 角色卡字段内定位
- 角色卡字段跳转时，除了打开并聚焦对应输入框，还会**选中第一处匹配并滚动到那一行**，不必再手动翻找。

## 1.6.1 — 楼层精跳 + 分类标签卡
- 「该角色聊天」改为**按消息**呈现：点击任意结果可直接跳到该聊天存档的**具体楼层**（非当前存档会先自动打开再定位）。
- 修复「（当前）」标记可能过时、误标、点击无法跳转的问题：改为**点击时实时**判断当前聊天，并去掉易过时的标记。
- 结果区上方新增**分类标签卡**（全部 / 预设条目 / 角色卡 / 世界书 / 聊天记录）：搜索后才出现，只显示有结果的类别，点击可只看该类。
- 无匹配的分组不再显示，整体更清爽。

## 1.6.0 — 角色搜索增强 + 跳转修复
- 「当前角色卡」范围扩展为「当前角色全家桶」：除角色卡字段外，新增搜索 **该角色绑定的世界书**（内嵌 character_book、主世界书、charLore 附加书）与 **该角色的全部聊天存档**（含当前），结果分组显示。
- 聊天跳转支持**高处 / 未渲染 / 隐藏的楼层**：跳转前自动加载更早的楼层再定位。
- 新增**该角色其它聊天存档的跳转**：点击自动打开对应存档。
- 角色卡字段跳转更可靠：定位前自动展开右侧角色面板或「高级定义」弹窗。

## 1.5.0 — 高级 UI 大改版
- 全新玻璃拟态界面：珠宝色渐变背景 + 半透明玻璃面板 + 高光描边与发光阴影。
- 新增 10 套可切换配色：黑金、粉紫、红蓝、祖母绿、深海、月白、樱粉、落日，外加「跟随酒馆主题」（读取主题强调色）和「跟随时间」（白天亮色系 / 夜间暗色系）。
- 配色通过面板右上角的圆形色卡一键切换，所选主题自动保存，刷新不丢。
- 搜索按钮重做为圆弧胶囊主按钮，点击有凹陷反馈。
- 正则模式旁新增「?」说明按钮，点击展开通俗解释。
- 适配窄屏，并支持「减少动效」无障碍设置。

## 1.4.0 — 界面初次美化
- 首版样式优化（随后被 1.5.0 的玻璃拟态界面全面取代）。

## 1.3.3 — 修复预设跳转
- 预设条目跳转改为直接调用提示词管理器实例打开编辑框，彻底解决「只弹提示、不定位」的问题；不再依赖列表是否渲染或面板是否打开。

## 1.3.2 — 预设跳转改进
- 跳转改用「编辑」动作（无条件载入条目内容），替代仅在该条目已被发送过才有效的「查看」动作。

## 1.3.1 — 预设跳转改进
- 找不到条目时自动展开「AI 响应配置」抽屉以触发列表渲染；改用属性精确匹配，避免条目名含特殊字符导致定位失败。

## 1.3.0 — 搜索历史 + 保留上次状态
- 新增最近 10 条搜索记录，可单条删除或一键清空，记录持久保存（刷新不丢）。
- 跳转后重新打开面板时，自动恢复上次的关键词、勾选范围与搜索结果。

## 1.2.0 — 结果点击跳转
- 四类「当前」结果支持点击跳转到所在位置并高亮：
  - 当前聊天：滚动定位到对应消息。
  - 世界书：自动打开对应世界书并定位条目。
  - 当前预设：定位并打开对应提示词条目。
  - 当前角色卡：定位并聚焦对应字段（必要时打开「高级定义」）。
- 全局结果（全部角色卡 / 全部聊天）因涉及切换上下文，暂不提供跳转。

---

1.1.3 及更早：基础关键词搜索、六类搜索范围（当前预设 / 世界书 / 当前角色卡 / 当前聊天 / 全部角色卡 / 全部聊天）、频次统计、区分大小写与正则模式、原生弹窗渲染与手机端适配等。
