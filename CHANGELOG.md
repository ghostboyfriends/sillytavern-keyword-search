# 更新日志

本扩展的版本更新记录（新版在上）。

## 1.5.0 — 高级 UI 大改版
- 全新玻璃拟态界面：珠宝色渐变背景 + 半透明玻璃面板 + 高光描边与发光阴影。
- 新增 10 套可切换配色：黑金、粉紫、红蓝、祖母绿、深海、月白、樱粉、落日，外加「跟随酒馆主题」（读取主题强调色）和「跟随时间」（白天亮色系 / 夜间暗色系）。
- 配色通过面板右上角的圆形色卡一键切换，所选主题自动保存，刷新不丢。
- 搜索按钮重做为圆弧胶囊主按钮，点击有凹陷反馈。
- 正则模式旁新增「?」说明按钮，点击展开通俗解释。
- 适配窄屏，并支持「减少动效」无障碍设置。

## 1.4.0 — 界面初次美化
- 首版样式优化（随后被 1.5.0 的玻璃拟态界面全面取代）。

## 1.3.3 — 修复预设跳转
- 预设条目跳转改为直接调用提示词管理器实例打开编辑框，彻底解决「只弹提示、不定位」的问题；不再依赖列表是否渲染或面板是否打开。

## 1.3.2 — 预设跳转改进
- 跳转改用「编辑」动作（无条件载入条目内容），替代仅在该条目已被发送过才有效的「查看」动作。

## 1.3.1 — 预设跳转改进
- 找不到条目时自动展开「AI 响应配置」抽屉以触发列表渲染；改用属性精确匹配，避免条目名含特殊字符导致定位失败。

## 1.3.0 — 搜索历史 + 保留上次状态
- 新增最近 10 条搜索记录，可单条删除或一键清空，记录持久保存（刷新不丢）。
- 跳转后重新打开面板时，自动恢复上次的关键词、勾选范围与搜索结果。

## 1.2.0 — 结果点击跳转
- 四类「当前」结果支持点击跳转到所在位置并高亮：
  - 当前聊天：滚动定位到对应消息。
  - 世界书：自动打开对应世界书并定位条目。
  - 当前预设：定位并打开对应提示词条目。
  - 当前角色卡：定位并聚焦对应字段（必要时打开「高级定义」）。
- 全局结果（全部角色卡 / 全部聊天）因涉及切换上下文，暂不提供跳转。

---

1.1.3 及更早：基础关键词搜索、六类搜索范围（当前预设 / 世界书 / 当前角色卡 / 当前聊天 / 全部角色卡 / 全部聊天）、频次统计、区分大小写与正则模式、原生弹窗渲染与手机端适配等。
