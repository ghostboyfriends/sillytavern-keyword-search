# 更新日志

本扩展的版本更新记录（新版在上）。

## 1.11.1 — 角色绑定世界书也能替换
- 修复：搜到的匹配在「角色世界书」分组（当前角色卡绑定的世界书）里时，替换提示「没有可替换的匹配」。
- 现在勾选「当前角色卡」后，替换也覆盖该角色**绑定的世界书**：命名世界书走 saveWorldInfo，内嵌 character_book 走官方接口写回。

## 1.11.0 — 世界书也能替换
- 替换范围新增「世界书」：勾选「世界书」后，可在「查找可替换项」里逐条勾选世界书条目进行替换。
- 世界书通过官方接口（saveWorldInfo）安全写回，可跨多本书批量替换。

## 1.10.2 — 再修预设跳转空白（重名条目）
- 修复：部分预设里存在 identifier 重复 / 为空的条目时，跳转会按 identifier 取到「同名的另一条空条目」导致编辑框空白。
- 现在跳转会用**搜索时真正命中的那条**（按下标精确定位，重名时挑内容确实包含关键词的那条）直接载入编辑表单，重名也能显示正确内容。

## 1.10.1 — 修复预设跳转空白
- 修复：预设条目跳转后编辑框内容空白的问题。跳转改为复刻手动操作（打开提示词管理器并点该条目的「编辑」），内容会被正确载入。
- 新增守卫：若搜索后切换了预设、再点旧的缓存结果，会提示「该条目已不在当前预设，请重新搜索」，而不是打开空白编辑框。

## 1.10.0 — 支持备用开场白
- 搜索：「当前角色卡 / 全部角色卡」现在会一并搜索角色的**备用开场白**（alternate greetings）。
- 替换：可对角色的**每条备用开场白**逐项替换。备用开场白通过官方接口（merge-attributes）安全写回，**无需打开任何面板**，也不会影响角色卡的其它字段。

## 1.9.1 — 替换体验修复 + 确认框美化
- 修复：在替换预览列表打开后再修改「替换为」的内容，现在会**实时生效**（之前会沿用点「查找可替换项」那一刻的旧值）。
- 确认弹窗换成与主面板一致的玻璃风格，并跟随当前主题配色（替换确认、预设切换确认均已美化）。

## 1.9.0 — 界面精简 + 可勾选替换
- 界面精简：不常用的「全局」范围（全部预设 / 全部角色卡 / 全部聊天）默认收起，点「全局」按钮再展开；常用范围更清爽。
- 替换不再只能「全部替换」：点「查找可替换项」会列出所有可替换目标（每条预设条目 / 每条消息 / 每个角色卡字段为一项，含命中数与高亮预览），可**逐项勾选**或**全选**，再「替换所选」。
- 替换前仍弹窗确认并提示备份；执行后保存并刷新。

## 1.8.0 — 查找替换
- 新增「替换」功能（点选项区的「替换」展开）：输入替换词后「全部替换」，类似 WPS 的查找替换。
- 替换**仅作用于已勾选的「当前预设 / 当前聊天 / 当前角色卡」**三处，遵循区分大小写 / 正则设置（正则可用 `$1` 反向引用）。
- 替换前弹窗确认，列出各处命中数量并提示先备份；替换后会直接保存数据并刷新搜索结果。
- 角色卡的高级定义字段若本次未打开过，相应替换会被安全跳过并提示（先打开一次角色「高级定义」即可）。

## 1.7.2 — 查看器界面美化
- 其它预设条目的查看器换上与主面板一致的玻璃面板，并**跟随主面板当前配色**（包括「跟随酒馆主题」）。
- 移除查看器底部多余的提示文字，新增右上角关闭按钮。

## 1.7.1 — 不切换预设也能查看其它预设条目
- 点击「全部预设」里**其它预设**的条目，默认弹出**只读查看器**直接看完整内容（带关键词高亮），**完全不改动当前预设**。
- 查看器内提供「切换到此预设并编辑」按钮，需要真正编辑时再走切换流程（仍会先确认是否保存当前预设）。

## 1.7.0 — 搜索全部预设
- 新增「全部预设」搜索范围：一次性搜索所有预设里的条目（只读取、不切换预设）。
- 跳转到**非当前预设**的条目时，会先弹窗确认：**保存当前并切换 / 不保存直接切换 / 取消**，避免误丢当前预设未保存的修改；切换后自动定位到目标条目。

## 1.6.2 — 角色卡字段内定位
- 角色卡字段跳转时，除了打开并聚焦对应输入框，还会**选中第一处匹配并滚动到那一行**，不必再手动翻找。

## 1.6.1 — 楼层精跳 + 分类标签卡
- 「该角色聊天」改为**按消息**呈现：点击任意结果可直接跳到该聊天存档的**具体楼层**（非当前存档会先自动打开再定位）。
- 修复「（当前）」标记可能过时、误标、点击无法跳转的问题：改为**点击时实时**判断当前聊天，并去掉易过时的标记。
- 结果区上方新增**分类标签卡**（全部 / 预设条目 / 角色卡 / 世界书 / 聊天记录）：搜索后才出现，只显示有结果的类别，点击可只看该类。
- 无匹配的分组不再显示，整体更清爽。

## 1.6.0 — 角色搜索增强 + 跳转修复
- 「当前角色卡」范围扩展为「当前角色全家桶」：除角色卡字段外，新增搜索 **该角色绑定的世界书**（内嵌 character_book、主世界书、charLore 附加书）与 **该角色的全部聊天存档**（含当前），结果分组显示。
- 聊天跳转支持**高处 / 未渲染 / 隐藏的楼层**：跳转前自动加载更早的楼层再定位。
- 新增**该角色其它聊天存档的跳转**：点击自动打开对应存档。
- 角色卡字段跳转更可靠：定位前自动展开右侧角色面板或「高级定义」弹窗。

## 1.5.0 — 高级 UI 大改版
- 全新玻璃拟态界面：珠宝色渐变背景 + 半透明玻璃面板 + 高光描边与发光阴影。
- 新增 10 套可切换配色：黑金、粉紫、红蓝、祖母绿、深海、月白、樱粉、落日，外加「跟随酒馆主题」（读取主题强调色）和「跟随时间」（白天亮色系 / 夜间暗色系）。
- 配色通过面板右上角的圆形色卡一键切换，所选主题自动保存，刷新不丢。
- 搜索按钮重做为圆弧胶囊主按钮，点击有凹陷反馈。
- 正则模式旁新增「?」说明按钮，点击展开通俗解释。
- 适配窄屏，并支持「减少动效」无障碍设置。

## 1.4.0 — 界面初次美化
- 首版样式优化（随后被 1.5.0 的玻璃拟态界面全面取代）。

## 1.3.3 — 修复预设跳转
- 预设条目跳转改为直接调用提示词管理器实例打开编辑框，彻底解决「只弹提示、不定位」的问题；不再依赖列表是否渲染或面板是否打开。

## 1.3.2 — 预设跳转改进
- 跳转改用「编辑」动作（无条件载入条目内容），替代仅在该条目已被发送过才有效的「查看」动作。

## 1.3.1 — 预设跳转改进
- 找不到条目时自动展开「AI 响应配置」抽屉以触发列表渲染；改用属性精确匹配，避免条目名含特殊字符导致定位失败。

## 1.3.0 — 搜索历史 + 保留上次状态
- 新增最近 10 条搜索记录，可单条删除或一键清空，记录持久保存（刷新不丢）。
- 跳转后重新打开面板时，自动恢复上次的关键词、勾选范围与搜索结果。

## 1.2.0 — 结果点击跳转
- 四类「当前」结果支持点击跳转到所在位置并高亮：
  - 当前聊天：滚动定位到对应消息。
  - 世界书：自动打开对应世界书并定位条目。
  - 当前预设：定位并打开对应提示词条目。
  - 当前角色卡：定位并聚焦对应字段（必要时打开「高级定义」）。
- 全局结果（全部角色卡 / 全部聊天）因涉及切换上下文，暂不提供跳转。

---

1.1.3 及更早：基础关键词搜索、六类搜索范围（当前预设 / 世界书 / 当前角色卡 / 当前聊天 / 全部角色卡 / 全部聊天）、频次统计、区分大小写与正则模式、原生弹窗渲染与手机端适配等。
